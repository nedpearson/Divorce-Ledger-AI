# ============================================
# DivorceASE AI - Replit Configuration
# ============================================

# Main entry point and build command
run = "npm run build && npm run start"
entrypoint = "dist/index.js"

# ============================================
# ENVIRONMENT VARIABLES
# ============================================
[env]
# Application
PORT = "5000"
HOST = "0.0.0.0"
NODE_ENV = "development"
LOG_TO_FILE = "false"

# Stripe Configuration
STRIPE_MODE = "test"
# NOTE: STRIPE_SECRET_KEY and STRIPE_PUBLIC_KEY must be set in Replit Secrets
# Do NOT hardcode API keys in this file!

# Database (Example - update with your actual values)
# DATABASE_URL = "postgresql://user:password@localhost:5432/divorceaseai"
# NOTE: Set DATABASE_URL in Replit Secrets if using cloud database

# Security (Generate strong secrets and store in Replit Secrets)
# SECRET_KEY = "generate-a-random-32-char-string"
# JWT_SECRET = "generate-a-random-32-char-string"
# SESSION_SECRET = "generate-a-random-32-char-string"

# ============================================
# NIX ENVIRONMENT
# ============================================
[nix]
channel = "stable-23.11"

# ============================================
# PACKAGE MANAGER
# ============================================
[packageManager]
npm = "npm@10.2.4"

# ============================================
# LANGUAGE SUPPORT
# ============================================
[languages.typescript]
pattern = "**/*.ts"
syntax = "typescript"

[languages.javascript]
pattern = "**/*.js"
syntax = "javascript"

[languages.json]
pattern = "**/*.json"
syntax = "json"

[languages.sql]
pattern = "**/*.sql"
syntax = "sql"

# ============================================
# DEBUGGER (Optional)
# ============================================
[debugger]
support = true

# ============================================
# PORTS
# ============================================
[ports]
# Main application port
[[ports.app]]
localPort = 5000
externalPort = 80
expose = true
protocol = "http"

# ============================================
# LINTER (Optional)
# ============================================
[linter]
args = ["--max-warnings=0"]

# ============================================
# FORMATTER (Optional)
# ============================================
[formatter]
command = "prettier"
args = ["--write", "."]
