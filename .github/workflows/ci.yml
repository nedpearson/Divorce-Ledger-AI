
name: CI - Continuous Integration

# Explicit permissions: only read access to contents and metadata
permissions:
  contents: read
  id-token: none
  issues: none
  pull-requests: none
  deployments: none
  statuses: none
  checks: none
  packages: none
  actions: none
  security-events: none
  discussions: none
  repository-projects: none
  pages: none
  attestations: none

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-check:
    name: Build & Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type check
        run: npm run check

      - name: Build application
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 3

  security-scan:
    name: Security - Dependency Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run npm audit
        run: npm audit --audit-level=high
        continue-on-error: true

  ci-success:
    name: CI - All Checks Passed
    runs-on: ubuntu-latest
    needs:
      - build-and-check
      - security-scan

    steps:
      - name: Summary
        run: |
          echo "✅ All CI checks passed!"
          echo "- TypeScript type check"
          echo "- Production build"
          echo "- Security scan"
